{
  "name": "scond",
  "version": "1.0.0",
  "description": "simple condition system",
  "keywords": ["scond", "validation", "conditions", "check"],
  "author": "Andrey Shuvalov <dronbas@gmail.com>",
  "license": "MIT",
  "main": "src/index",
  "scripts": {
    "test": "nyc mocha",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "precommit": "lint-staged",
    "prepush": "npm test"
  },
  "lint-staged": {
    "*.{js,json,css}": [
      "prettier --trailing-comma all --write '{src,test}/**/*.js'",
      "git add"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dronbas/scond.git"
  },
  "engines": {
    "node": ">=6"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "codecov": "^3.0.0",
    "husky": "^0.14.3",
    "lint-staged": "^4.3.0",
    "mocha": "^4.0.1",
    "nyc": "^11.2.1",
    "prettier": "^1.7.4"
  }
}
